// Function to add two numbers
function addNumbers(num1, num2) {
    return num1 + num2;
}

// Example usage
const number1 = 5; // First number
const number2 = 10; // Second number

const sum = addNumbers(number1, number2);
console.log(`The sum of ${number1} and ${number2} is: ${sum}`);
